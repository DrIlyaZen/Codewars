import math
def is_prime(num):
    print(num)
    if(num==2):return True
    if((num<=0)or(num==1)or(num%2==0)):return False
    for x in range(2,int(math.sqrt(num)+1)):
        if(num%x==0):return False
    return True
